services:
  mongo:
    image: mongo:7
    container_name: franquicias-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: franquiciasdb
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
